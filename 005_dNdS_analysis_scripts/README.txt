To run dnds analysis.

a. first copy all 3 script files and the ctl template files.
b. keep "query" fasta (Must be CDS) in "query" folder.
c. keep "target" fasta files (Must be CDS) in "targets" folder.
d. all file names needed to be simplified (AedesAegypti.fasta)
e. run scripts one by one.